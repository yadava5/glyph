# Optional backend HTTP API base URL. Leave unset for static demo mode.
# VITE_API_BASE_URL=http://localhost:8080

# Enable only when web/public/wasm contains fast_mnist.js, fast_mnist.wasm,
# and model.weights.bin.
# VITE_ENABLE_WASM=true
